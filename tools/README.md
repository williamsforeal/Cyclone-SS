# Tools

Python scripts for deterministic execution (WAT Layer 3).

Each script handles one task: API calls, data transformations, file operations, database queries.

## Rules
- Credentials and API keys come from `.env`
- Scripts should be consistent, testable, and fast
- Check here before building anything new
